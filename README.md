# 一维细胞粘弹性基底铺展的 Kinetic Monte Carlo 强耦合模型求解

## 任务背景
细胞在 Standard Linear Solid (SLS) 粘弹性基底上的铺展是一个涉及多尺度转换的强耦合过程。细胞分子离合器 (Motor Clutch Model) 的动态随机解离（毫秒级生化反应）与基底宏观粘弹力学松弛（秒级连续响应）相互激荡。本任务要求你复现这一跨尺度的力学演化模型。

## 核心控制方程
1. **基底 SLS 粘弹性演化连续方程：**
2. **分子离合器方程**
3. Gillepse算法
4. 
## 你的具体任务
打开 `scaffold.py`，你将看到除了基础的物理常量声明外，主循环部分已被完全清空。你必须在 `[Agent 核心任务区]` **从零构建** 完整的求解器逻辑：
1. **构建 Gillespie (KMC) 随机抽样核心**：正确计算出所有结合/未结合离合器的变动概率，通过双随机数抽样决定发生事件的离合器索引以及自适应随机时间步长 $dt$。
2. **粘弹性引入**：根据SLS粘弹性模型，引入基质粘弹性对Motor Clutch位移的应力松弛效应。

## 约束与规范
* 函数接口必须保持 `vis_MC_Gillespie_Elastic_SLS(neta, ka, kl=0.1, is_plot=False)` 不变。
* 计算流必须完全离线运行，严禁调用任何外部的非原生科学计算包。
* 必须使用纯粹的、严格基于第一性原理进行推导。
* 严禁使用任何 np.sin、Sigmoid、高斯函数或经验公式来人工合成时域信号。 必须完全依赖微观状态的随机抽样来自然演化出宏观均值和周期。
